# `@knownpath/canonicalization`

Versioned technical normalization, deterministic blocking and pair comparison, conservative merge
policy, auditable candidate memberships, and stable KnownPath regeneration.

See [`docs/CANONICALIZATION.md`](../../docs/CANONICALIZATION.md).
