# `@knownpath/verification`

Resolves extracted candidate claims against immutable source snapshots, records explicit evidence
signals, and creates immutable deterministic seed-confidence assessments. It never calls an AI
provider and never mutates an existing assessment.
