{
  "name": "@knownpath/source-ingestion",
  "version": "0.0.0",
  "private": true,
  "description": "Official documentation and release-feed collection for KnownPath.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@knownpath/config": "workspace:*",
    "@knownpath/database": "workspace:*",
    "@knownpath/domain": "workspace:*",
    "@knownpath/observability": "workspace:*",
    "fast-xml-parser": "catalog:",
    "html-to-text": "catalog:",
    "ipaddr.js": "catalog:",
    "marked": "catalog:",
    "robots-parser": "catalog:",
    "undici": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@knownpath/typescript-config": "workspace:*",
    "@types/html-to-text": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
