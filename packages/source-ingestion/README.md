# `@knownpath/source-ingestion`

Discovers and incrementally synchronizes configured first-party documentation and release feeds. It
normalizes untrusted source material and persists immutable snapshots plus mutable fetch state.

This package does not extract solutions or create KnownPaths.
