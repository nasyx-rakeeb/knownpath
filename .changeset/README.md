# Changesets

Changesets records user-visible changes to the public `knownpath` package. Internal `@knownpath/*`
workspaces remain private and are not versioned or published.

Add a changeset with `pnpm changeset`, inspect pending releases with `pnpm release:status`, and
follow `docs/RELEASE.md` before running version or publish commands.
